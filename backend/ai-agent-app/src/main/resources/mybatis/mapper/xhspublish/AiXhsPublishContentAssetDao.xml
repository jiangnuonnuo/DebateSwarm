<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dasi.infrastructure.dao.xhspublish.IAiXhsPublishContentAssetDao">
    <resultMap id="dataMap" type="com.dasi.infrastructure.dao.po.xhspublish.AiXhsPublishContentAsset">
        <id column="id" property="id"/>
        <result column="asset_id" property="assetId"/>
        <result column="task_id" property="taskId"/>
        <result column="attempt_id" property="attemptId"/>
        <result column="user_id" property="userId"/>
        <result column="asset_type" property="assetType"/>
        <result column="source_type" property="sourceType"/>
        <result column="origin_url" property="originUrl"/>
        <result column="storage_type" property="storageType"/>
        <result column="storage_ref" property="storageRef"/>
        <result column="access_url" property="accessUrl"/>
        <result column="sort_no" property="sortNo"/>
        <result column="asset_status" property="assetStatus"/>
        <result column="expire_time" property="expireTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    <select id="queryByAssetId" resultMap="dataMap">
        SELECT
               id,
               asset_id,
               task_id,
               attempt_id,
               user_id,
               asset_type,
               source_type,
               origin_url,
               storage_type,
               storage_ref,
               access_url,
               sort_no,
               asset_status,
               expire_time,
               create_time,
               update_time
        FROM ai_xhs_publish_content_asset
        WHERE asset_id = #{assetId}
    </select>

    <select id="listByTaskId" resultMap="dataMap">
        SELECT
               id,
               asset_id,
               task_id,
               attempt_id,
               user_id,
               asset_type,
               source_type,
               origin_url,
               storage_type,
               storage_ref,
               access_url,
               sort_no,
               asset_status,
               expire_time,
               create_time,
               update_time
        FROM ai_xhs_publish_content_asset
        WHERE task_id = #{taskId}
        ORDER BY sort_no ASC, id ASC
    </select>

    <select id="listByAttemptId" resultMap="dataMap">
        SELECT
               id,
               asset_id,
               task_id,
               attempt_id,
               user_id,
               asset_type,
               source_type,
               origin_url,
               storage_type,
               storage_ref,
               access_url,
               sort_no,
               asset_status,
               expire_time,
               create_time,
               update_time
        FROM ai_xhs_publish_content_asset
        WHERE attempt_id = #{attemptId}
        ORDER BY sort_no ASC, id ASC
    </select>

    <select id="listExpired" resultMap="dataMap">
        SELECT
               id,
               asset_id,
               task_id,
               attempt_id,
               user_id,
               asset_type,
               source_type,
               origin_url,
               storage_type,
               storage_ref,
               access_url,
               sort_no,
               asset_status,
               expire_time,
               create_time,
               update_time
        FROM ai_xhs_publish_content_asset
        WHERE expire_time IS NOT NULL
          AND expire_time &lt;= #{expireTime}
        ORDER BY expire_time ASC, id ASC
    </select>
    <insert id="insert" parameterType="com.dasi.infrastructure.dao.po.xhspublish.AiXhsPublishContentAsset">
        INSERT INTO ai_xhs_publish_content_asset (
                             asset_id,
                             task_id,
                             attempt_id,
                             user_id,
                             asset_type,
                             source_type,
                             origin_url,
                             storage_type,
                             storage_ref,
                             access_url,
                             sort_no,
                             asset_status,
                             expire_time)
        VALUES (
                #{assetId},
                #{taskId},
                #{attemptId},
                #{userId},
                #{assetType},
                #{sourceType},
                #{originUrl},
                #{storageType},
                #{storageRef},
                #{accessUrl},
                #{sortNo},
                #{assetStatus},
                #{expireTime})
    </insert>

    <update id="update" parameterType="com.dasi.infrastructure.dao.po.xhspublish.AiXhsPublishContentAsset">
        UPDATE ai_xhs_publish_content_asset
        <set>
            <if test="assetId != null">asset_id = #{assetId},</if>
            <if test="taskId != null">task_id = #{taskId},</if>
            <if test="attemptId != null">attempt_id = #{attemptId},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="assetType != null">asset_type = #{assetType},</if>
            <if test="sourceType != null">source_type = #{sourceType},</if>
            <if test="originUrl != null">origin_url = #{originUrl},</if>
            <if test="storageType != null">storage_type = #{storageType},</if>
            <if test="storageRef != null">storage_ref = #{storageRef},</if>
            <if test="accessUrl != null">access_url = #{accessUrl},</if>
            <if test="sortNo != null">sort_no = #{sortNo},</if>
            <if test="assetStatus != null">asset_status = #{assetStatus},</if>
            <if test="expireTime != null">expire_time = #{expireTime},</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>

