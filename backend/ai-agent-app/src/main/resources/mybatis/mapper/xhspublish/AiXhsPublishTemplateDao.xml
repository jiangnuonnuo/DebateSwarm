<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dasi.infrastructure.dao.xhspublish.IAiXhsPublishTemplateDao">
    <resultMap id="dataMap" type="com.dasi.infrastructure.dao.po.xhspublish.AiXhsPublishTemplate">
        <id column="id" property="id"/>
        <result column="template_id" property="templateId"/>
        <result column="user_id" property="userId"/>
        <result column="template_name" property="templateName"/>
        <result column="publish_mode" property="publishMode"/>
        <result column="template_config_json" property="templateConfigJson"/>
        <result column="template_status" property="templateStatus"/>
        <result column="is_default" property="isDefault"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    <select id="queryByTemplateId" resultMap="dataMap">
        SELECT
               id,
               template_id,
               user_id,
               template_name,
               publish_mode,
               template_config_json,
               template_status,
               is_default,
               create_time,
               update_time
        FROM ai_xhs_publish_template
        WHERE template_id = #{templateId}
    </select>

    <select id="page" resultMap="dataMap">
        SELECT
               id,
               template_id,
               user_id,
               template_name,
               publish_mode,
               template_config_json,
               template_status,
               is_default,
               create_time,
               update_time
        FROM ai_xhs_publish_template
        <where>
            AND user_id = #{userId}
            <if test="keyword != null and keyword != ''">
                AND template_name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY update_time DESC, id DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="count" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM ai_xhs_publish_template
        <where>
            AND user_id = #{userId}
            <if test="keyword != null and keyword != ''">
                AND template_name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
    </select>
    <insert id="insert" parameterType="com.dasi.infrastructure.dao.po.xhspublish.AiXhsPublishTemplate">
        INSERT INTO ai_xhs_publish_template (
                             template_id,
                             user_id,
                             template_name,
                             publish_mode,
                             template_config_json,
                             template_status,
                             is_default)
        VALUES (
                #{templateId},
                #{userId},
                #{templateName},
                #{publishMode},
                #{templateConfigJson},
                #{templateStatus},
                #{isDefault})
    </insert>

    <update id="update" parameterType="com.dasi.infrastructure.dao.po.xhspublish.AiXhsPublishTemplate">
        UPDATE ai_xhs_publish_template
        <set>
            <if test="templateId != null">template_id = #{templateId},</if>
            <if test="userId != null">user_id = #{userId},</if>
            <if test="templateName != null">template_name = #{templateName},</if>
            <if test="publishMode != null">publish_mode = #{publishMode},</if>
            <if test="templateConfigJson != null">template_config_json = #{templateConfigJson},</if>
            <if test="templateStatus != null">template_status = #{templateStatus},</if>
            <if test="isDefault != null">is_default = #{isDefault},</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>

