<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dasi.infrastructure.persistent.dao.IAiMeetingActionDao">

    <resultMap id="dataMap" type="com.dasi.infrastructure.persistent.po.AiMeetingAction">
        <id column="id" property="id"/>
        <result column="action_id" property="actionId"/>
        <result column="meeting_id" property="meetingId"/>
        <result column="room_id" property="roomId"/>
        <result column="action_type" property="actionType"/>
        <result column="actor_id" property="actorId"/>
        <result column="actor_role" property="actorRole"/>
        <result column="target_id" property="targetId"/>
        <result column="target_role" property="targetRole"/>
        <result column="payload" property="payload"/>
        <result column="reason" property="reason"/>
        <result column="required_fix" property="requiredFix"/>
        <result column="deadline" property="deadline"/>
        <result column="result" property="result"/>
        <result column="meeting_version" property="meetingVersion"/>
        <result column="client_request_id" property="clientRequestId"/>
        <result column="trace_id" property="traceId"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.dasi.infrastructure.persistent.po.AiMeetingAction">
        INSERT INTO ai_meeting_action (
            action_id, meeting_id, room_id, action_type, actor_id, actor_role,
            target_id, target_role, payload, reason, required_fix, deadline, result,
            meeting_version, client_request_id, trace_id
        ) VALUES (
            #{actionId}, #{meetingId}, #{roomId}, #{actionType}, #{actorId}, #{actorRole},
            #{targetId}, #{targetRole}, #{payload}, #{reason}, #{requiredFix}, #{deadline}, #{result},
            #{meetingVersion}, #{clientRequestId}, #{traceId}
        )
    </insert>

    <select id="queryByClientRequestId" resultMap="dataMap">
        SELECT id, action_id, meeting_id, room_id, action_type, actor_id, actor_role,
               target_id, target_role, payload, reason, required_fix, deadline, result,
               meeting_version, client_request_id, trace_id, create_time
        FROM ai_meeting_action
        WHERE meeting_id = #{meetingId}
          AND client_request_id = #{clientRequestId}
          AND is_deleted = 0
        LIMIT 1
    </select>

    <select id="queryTimelineByMeetingId" resultMap="dataMap">
        SELECT id, action_id, meeting_id, room_id, action_type, actor_id, actor_role,
               target_id, target_role, payload, reason, required_fix, deadline, result,
               meeting_version, client_request_id, trace_id, create_time
        FROM ai_meeting_action
        WHERE meeting_id = #{meetingId}
          AND is_deleted = 0
        ORDER BY create_time ASC, id ASC
        LIMIT #{limit}
    </select>

</mapper>

